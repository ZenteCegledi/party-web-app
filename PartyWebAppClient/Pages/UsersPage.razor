@using System.Runtime.InteropServices.JavaScript
@using PartyWebAppClient.Services
@using PartyWebAppCommon
@page "/admin/users"

<!--<FluentDataGrid Items = "@users">
    <PropertyCloumn Property = "@(d => d.Username)" />
    <PropertyColumn Property = "@(d => d.BirthDate)" />
    <PropertyColumn Property = "@(d => d.Email)" />
    <PropertyColumn Property = "@(d => d.Phone)"/>
    <PropertyColumn Property = "@(d => d.Password)" />
    <PropertyCOlumn Property = "@(d => d.Wallets)" />
</FluentDataGrid>-->

<!-- [Key]
    public string Username { get; set; }

    public string Name { get; set; }
    public DateTime BirthDate { get; set; }
    public string Email { get; set; }
    public string Phone { get; set; }
    public string Password { get; set; }

    public List<Wallet> Wallets { get; set; } -->
@code{
    // IQueryable<User> users = 
}